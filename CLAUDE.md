# CLAUDE.md

This file exists only so Claude Code auto-loads project guidance. AGENTS.md is the source
of truth (architecture, conventions, known gotchas, open optimizations) — this just imports
it so there's one document to keep up to date, not two.

@AGENTS.md
